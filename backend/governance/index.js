'use strict';const{createRouter}=require('./router');const{sequelize}=require('./store');const{evaluate}=require('./domain');const{sequelize:db}=require('../models');const{authenticateToken:auth}=require('../middleware/auth');module.exports=createRouter({db:sequelize(db),auth,evaluate,workflow:'broadcast-creation',providers:['media-model','asset-library','object-storage','cdn','transcription','translation','publishing'],approverRoles:['editor','rights_reviewer','brand_reviewer','admin']});
